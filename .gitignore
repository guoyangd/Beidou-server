/logs/**
.idea/*
!.idea/codeStyles/
*.iml
/target
.github/*
!.github/ISSUE_TEMPLATE/
!.github/workflows/
.vscode

# build files
/build/
/dist/
/nbproject/
/.settings
/out
*.onetoc2

# Eclipse m2e generated files
# Eclipse Core
.project
# JDT-specific (Eclipse Java Development Tools)
.classpath

# Database
database/docker-db-data
database/docker-pg-db-data

# macOS files
.DS_Store

# Java project specific
main/resources/static/
src/main/resources/static/
.claude/settings.local.json
CLAUDE.local.md

# zcode agent local data
.zcode/

# docker runtime data (mysql data / backups / server volume)
docker/data/

# maven build output
gms-server/target/
gms-server/cache/

# 部署包解压副产物与 npm 生成的锁文件(项目用 yarn)
/BeiDou.jar
gms-ui/package-lock.json

# 部署发布包(二进制, 不入库; 由 CI/手工从源码构建)
/beidou-bot-deploy.tar.gz
